{
    "compilerOptions": {
        // type checking everywhere
        "strict": true,
        // which syntax we using
        "target": "esnext",
        // the way we import things
        "module": "nodenext",
        // where to gen js
        "outDir": "../../dist-electron",
        // skips libraries error cuz who gives a fuck
        "skipLibCheck": true
    }
}